CREATE TABLE estudiantes (
    ID     BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE   VARCHAR(255),
    APELLIDOS VARCHAR(255),
    CORREO VARCHAR(255),
    DNI VARCHAR(255),
    OWNER VARCHAR(256)
);